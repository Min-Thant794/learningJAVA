package functions;

public class Function1 {
		/*
		 * There are 4 access modifiers in Java
		 * 1. private (can only be used within its own class)
		 * 2. default (can be used within same package)
		 * 3. protected (can be used by same package and child class)
		 * 4. public (can be used everywhere)
		 * */
		
		/*
		 * Static Modifier
		 * Static => owned by class (className.)
		 * all of this shared by object (objectName.)
		 * */
		
		/*
		 * Instance
		 * owned by Object (ObjectName.)
		 * Object must be build first
		 * ClassName objectName = new ClassName(Value)
		 * String s = new String("Hello");
		 * */
}
